version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "5050:5050"
    environment:
      - PORT_NUMBER=5050 # Replace with your environment variables if needed
